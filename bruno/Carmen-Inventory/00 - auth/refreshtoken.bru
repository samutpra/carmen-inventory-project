meta {
  name: refreshtoken
  type: http
  seq: 4
}

post {
  url: {{host}}/api/v1/auth/refresh
  body: json
  auth: none
}

body:json {
  {
    "refresh" : "{{login_jwt}}"
  }
}

script:post-response {
  bru.setEnvVar('login_jwt',res.body.accessToken)
}
